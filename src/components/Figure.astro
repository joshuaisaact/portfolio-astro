---
import { Image } from "astro:assets";

interface Props {
  src: ImageMetadata;
  alt: string;
  title?: string;
}

const { src, alt, title } = Astro.props;
---

<figure class="my-8 flex flex-col items-center">
  <Image
    src={src}
    alt={alt}
    class="rounded-lg max-w-full h-auto"
    loading="lazy"
    width={800}
  />
  {
    title && (
      <figcaption class="text-center text-sm text-gray-400 dark:text-white mt-2 italic">
        {title}
      </figcaption>
    )
  }
</figure>
