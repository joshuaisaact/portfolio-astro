---
import { Image } from "astro:assets";

interface Props {
  src: ImageMetadata;
  alt: string;
  title?: string;
}

const { src, alt, title } = Astro.props;
---

<figure class="my-8 flex flex-col items-center">
  <Image
    src={src}
    alt={alt}
    class="rounded-lg max-w-full h-auto outline outline-1 -outline-offset-1 outline-black/10 dark:outline-white/10"
    loading="lazy"
    width={800}
  />
  {
    title && (
      <figcaption class="text-center text-sm text-gray-400 dark:text-white mt-2 italic">
        {title}
      </figcaption>
    )
  }
</figure>
