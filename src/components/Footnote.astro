---
interface Props {
  id: string | number;
}

const { id } = Astro.props;
---

<sup class="footnote-ref">
  <a id={`fnref-${id}`} href={`#fn-${id}`} class="text-primary dark:text-blue-400 hover:underline">
    [{id}]
  </a>
</sup>
