---
const { id, headingId, title } = Astro.props;
---

<section
  id={id}
  aria-labelledby={headingId}
  class="relative py-8 sm:py-16 md:py-20 border-t border-gray-200 dark:border-gray-800 px-0 sm:px-4 md:px-8"
>
  <div class="space-y-6 sm:space-y-8">
    <h2
      id={id}
      class="text-3xl font-semibold sm:text-4xl bg-linear-to-b from-gray-900 to-gray-700 bg-clip-text text-transparent dark:from-gray-100 dark:to-gray-300"
    >
      {title}
    </h2>
    <div role="region" aria-label={title}>
      <slot />
    </div>
  </div>
</section>
